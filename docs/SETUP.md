# Local Setup

Requirements:

- Python 3.12+
- uv installed
- bun installed

Setup:
make setup

Backend only:
make api-setup

Frontend only:
make web-setup

This creates a virtual environment and installs all dependencies.
